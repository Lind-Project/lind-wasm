{
  "default_route": "local",
  "remotes": {
    "zlib_server": { "endpoint": "unix:///tmp/zlib_remote.sock" }
  },
  "routes": [
    {
      "comment":  "intercept crc32 and delegate to remote zlib server",
      "symbol":   "crc32",
      "route":    "remote",
      "remote":   "zlib_server",
      "call_id":  1,
      "args": [
        { "type": "scalar" },
        { "type": "ptr", "direction": "in", "size_arg": 2 },
        { "type": "scalar" }
      ]
    },
    {
      "comment":  "intercept adler32 and delegate to remote zlib server",
      "symbol":   "adler32",
      "route":    "remote",
      "remote":   "zlib_server",
      "call_id":  2,
      "args": [
        { "type": "scalar" },
        { "type": "ptr", "direction": "in", "size_arg": 2 },
        { "type": "scalar" }
      ]
    }
  ]
}
