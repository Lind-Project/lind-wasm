
#include <math.h>

long double __floorl(long double x) {
    // Use the floorl function from math.h which computes the largest integer value less than or equal to x, for long double types
    return floorl(x);
}

