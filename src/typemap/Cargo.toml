[package]
name = "typemap"
version = "0.1.0"
edition = "2021"

[dependencies]
libc = "0.2"
fdtables = { path = "../fdtables", default-features = false }
cage = { path = "../cage" }
sysdefs = { path = "../sysdefs" }
goblin = "0.10.7"

[features]
default = ["fast"]
fast = []
secure = []
